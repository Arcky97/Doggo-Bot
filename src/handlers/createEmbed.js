const { EmbedBuilder } = require("discord.js")

module.exports = async (message, color, author, authorUrl, authorIconUrl, title, titleUrl, description, fields, imageUrl, thumbnailUrl, footer, footerIconUrl, timeStamp) => {
  
}